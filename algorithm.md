# Drop down algorithm
- hide the menu
- display the hamburger 
- if hamburger is cllicked fire an event that displays the menu
- display cancel (and the menu)
- if cancel is clicked fire event that removes menu